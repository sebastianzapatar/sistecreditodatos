name: Databricks Sync

on:
  push:
    branches: [ main ]  # ← RAMA CORRECTA
  pull_request:
    branches: [ main ]

jobs:
  sync-databricks:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.MY_PAT }}
        
    - name: Find Databricks Repo ID
      run: |
        echo "🔍 Buscando repositorio..."
        # Tu código para encontrar Repo ID
